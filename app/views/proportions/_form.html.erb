<%= simple_form_for([product, proportion], remote: true) do |f| %>
      <%= f.association :material, input_html: { class: "beautiful-dropdown" }, include_hidden: false %>
      <%= f.input :percentage %>
      <%= f.button :submit, class: "btn btn-primary", id: "new_proportion", value: "Add material" %>
    <% end %>
